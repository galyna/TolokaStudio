
@if(Request.IsAuthenticated) {
    <text>Вітаємо <strong>@User.Identity.Name</strong>

     @Html.ActionLink("Вийти", "LogOff", "Account") </text>
    if (User.IsInRole("Author"))
    {
         @Html.ActionLink("Кабінет", "Edit", "Employee")
    }
}
else {
     @Html.ActionLink("Увійти", "LogOn", "Account") 
}

