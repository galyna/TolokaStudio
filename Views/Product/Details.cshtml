@model Core.Data.Entities.Store
@{
    ViewBag.Title = "Деталі";
}
    @foreach (var item in Model.Products)
    {

        @Html.DisplayTextFor(modelItem => item.HtmlDetail)
       
    }
@*

